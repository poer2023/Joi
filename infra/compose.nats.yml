services:
  nats:
    image: nats:2.10-alpine
    container_name: agentos-nats
    restart: unless-stopped
    command: ["-js", "-m", "8222", "-sd", "/data"]
    ports:
      - "4222:4222"
      - "8222:8222"
    volumes:
      - nats_data:/data

volumes:
  nats_data:
