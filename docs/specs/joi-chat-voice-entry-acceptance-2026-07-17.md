# Joi Chat Voice Entry Acceptance — 2026-07-17

## Scope

- Project: `/Users/hao/project/Joi`
- Installed target: `/Applications/Joi.app`
- Screens: Settings → Chat Entry → Voice; main chat composer and assistant message actions
- User job: configure voice behavior in Settings, then record/transcribe or listen inside the chat where the feature is used.

## Information Structure

- Settings keeps only persistent voice preferences: macOS voice, speaking rate, local Whisper model, and transcription language.
- Chat composer owns microphone recording and local transcription.
- Assistant message actions own text-to-speech playback.
- Remove the Video tab, video generation, video understanding, and their exposed runtime capabilities.
- Keep ordinary file/image/video attachments unchanged; removing video understanding must not block attaching a video as a file.

## Forbidden Additions

- No media test workbench in Settings.
- No sample TTS text area, recording controls, result inspector, video prompt, or video-analysis picker in Settings.
- No automatic microphone start, automatic reply playback, or background recording.
- No unrelated changes to the current Settings visual-unification or title-bar-divider work.
- No legacy video execution module, IPC action, settings panel, historical video action label, or test fixture that can be mistaken for a supported product capability.
- Retired video capability identifiers may appear only in the store tombstone set or compiler regression assertion that prevents old SQLite rows from becoming callable again.

## Cleanup Boundary

- `/Applications/Joi.app` is the only retained runnable `Joi.app` bundle after verification.
- Repository build bundles may be regenerated by packaging and are removed after the installed bundle is verified; the release ZIP may remain.
- Old bundles under `~/.joi/runtime`, `.local/app-archive-*`, and the Trash are removed.
- Preserve `/Users/hao/Library/Application Support/Joi` and all conversation, memory, settings, and attachment data.
- The source deletion must be committed and pushed so a checkout does not restore the retired implementation.

## Interaction Rules

- The microphone button is explicit and toggles between start and stop.
- Stopping saves the recording locally, attaches it to the current composer, transcribes it locally, and places the transcript in the composer for review before sending.
- Permission, recording, saving, and transcription failures remain visible and do not send a message automatically.
- The assistant message “朗读” action uses the saved voice/rate and offers a second click to stop playback.

## Verification

- Targeted source tests assert the settings/chat split and absence of video capability exposure.
- The Electron and frontend packages type-check/build successfully.
- The packaged app replaces `/Applications/Joi.app`; codesign and source-to-bundle checks pass.
- Installed-app evidence shows:
  - Settings contains Voice preferences only, with no Video tab.
  - Chat composer contains a microphone entry.
  - Assistant messages expose a read-aloud action.
- A filesystem audit finds no runnable `Joi.app` other than `/Applications/Joi.app`.
- A source audit finds only documented retirement guards/regression assertions for the retired video capability IDs.

## Done Means

- [ ] Voice preferences persist and are consumed by chat actions.
- [ ] Recording/transcription is available only from chat.
- [ ] Read-aloud is available on assistant messages.
- [ ] Video generation and video understanding are absent from UI and callable capability exposure.
- [ ] Existing unrelated local changes remain intact.
- [ ] The installed app visually matches this contract.
- [ ] Old app bundles and unpacked build copies are removed without touching Joi user data.
- [ ] The cleanup commit is present on the tracked remote branch.
