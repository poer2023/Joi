# Joi 右侧观察面板审计（2026-07-15）

1. **旧项目人格编辑器 — 已清理 / 健康**
   - 旧界面把名称、项目名、本地路径、描述、自述、规则和保存按钮全部塞在日常概览中，其中 `/Users/hao/project/joi-desktop` 已不是当前仓库。
   - 当前概览只保留 Joi、状态、可用记忆、回复模型与本轮记忆入口；旧数据未从 SQLite 删除。

2. **固定线程页签 — 已清理 / 健康**
   - 右侧线程页与左侧线程导航重复，还混入旧导入线程。
   - 固定入口已收敛为 `概览 / 运行 / 产物 / 记忆`；需要定位来源时直接回到消息，而不是再打开一层线程面板。

3. **原始运行流水 — 已收敛 / 健康**
   - 日常运行页不再展示 `run.started`、`turn.started`、`model.delta`、内部上下文链接或高级 span 表。
   - 当前页只展示状态、模型调用数、动作数、最近动作和完整执行过程入口。
   - 同时修复空错误对象被误判为失败的问题；安装版中两个网页动作均显示“已完成”。

4. **文件页语义 — 已更新 / 健康**
   - `文件` 已改为更符合上传与生成结果的 `产物`，继续按当前对话筛选真实附件和生成物。

5. **记忆噪声 — 已清理 / 健康**
   - 标题与正文不再重复，内部英文原因串已映射为用户可读原因，匹配度封顶 100%。
   - 与当前单 Joi 版本冲突的“群主/五项目人格”和“五入口预览约束”不再进入右侧面板；底层记录保持可追溯。

6. **点击与展开反馈 — 已补齐 / 健康**
   - 面板 180 ms 右移淡入，内容 160 ms 轻移淡入；页签、观察按钮和面板按钮具备按压缩放反馈。
   - 展开态按钮有 active 状态和正确 `aria-expanded`；`prefers-reduced-motion` 下新增动画关闭。
   - 安装版实际点击四页签、概览到记忆联动、收起和重新展开均通过。

## 证据

- `01-overview-before.jpeg` 至 `05-memory-before.jpeg`：旧安装版逐页审计。
- `06-installed-collapsed-after.jpeg` 至 `10-installed-memory-after.jpeg`：最终安装版状态。
- `11-before-after-right-panel-comparison.png`：右侧面板前后同屏对照。
