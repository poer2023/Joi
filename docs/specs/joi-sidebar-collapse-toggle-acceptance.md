# Joi 侧栏折叠/展开验收合同

## 范围

- 修复窄窗口触发自动折叠后，点击「展开侧边栏」仍保持折叠的问题。
- 折叠态只保留一个明确的展开按钮，避免新建、搜索和展开三个按钮挤在标题栏。
- 不改变线程数据、当前会话、右侧观察面板和模型设置。

## 验收

- [x] 安装版折叠后可见且可点击「展开侧边栏」。
- [x] 用户主动展开时覆盖窄窗口自动折叠，侧栏重新出现。
- [x] 再次点击「折叠侧边栏」可以收起。
- [x] 重启后仍按默认自动布局启动，不持久化临时展开状态。

## 安装版证据

- 2026-07-10 在 `/Applications/Joi.app` 完成真实鼠标往返：`折叠侧边栏` → 折叠态仅保留 `展开侧边栏` → 点击后恢复 43 条线程列表。
- 前端构建、单 Agent 多线程测试和 macOS Electron 打包均通过；安装包签名校验通过。
