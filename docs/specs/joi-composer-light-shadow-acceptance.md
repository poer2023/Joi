# Joi Composer Light Shadow Acceptance

- [x] Composer keeps the current floating position, width, height, and internal controls.
- [x] Composer outer border reads lighter than the previous Tolaria adapter border.
- [x] Composer shadow is reduced to a shallow floating effect without a heavy glow.
- [x] Frontend build completes.
- [x] Browser preview confirms the computed composer border and box-shadow are updated.

Verified in the in-app browser at `http://127.0.0.1:5173/`: border remains `1px`, radius remains `12px`, and shadow resolves to two low-alpha layers.
