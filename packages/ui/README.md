# Joi UI

Shared UI primitives for Joi Desktop and Server Mode Console.

The first desktop PoC keeps the UI local to `apps/joi-desktop/frontend`. Components should move here only when they are reused by both Desktop Mode and Server Mode.
